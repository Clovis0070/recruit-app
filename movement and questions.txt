5.16:

    1. 跨域问题：
        配置 package.json 的 proxy 后，发现从浏览器 debug - network 看到的信息响应是 304，并且请求行业没有变，是否跨域失败？

        找到解答：跨域没有问题，消息已经成功发到服务端，并且服务端返回的是 200，不过浏览器看到的 304，可能是由于使用代理？


5.17:

    前后端使用 redux 管理状态的逻辑需要一个时间梳理一下。
    比如 一个注册的实现中，前后端每个文件分别怎么做的，做了哪些工作，以及最好明白怎么做有什么好处。 弄清楚这些，才能帮自己在独立使用 redux 写功能时能够有清醒的认识和逻辑。